---
const features = [{
  label: '数据连接器，集成业务数据',
  desc: ['套件内置数据连接器，帮助用户跨平台导入外部数据；', '允许用户设置例行任务，以自动同步和更新数据，确保数据时效性。']
}, {
  label: '脚本编辑器，记录操作指令',
  desc: ['支持把用户的操作记录翻译成 Univer 脚本语言 Uniscript，实现自动化操作；', '更可以让 AI 学习操作命令并生成脚本和操作提示，让工作更智能。']
}, {
  label: '高扩展性，适配个性化需求',
  desc: ['提供丰富插件，用户可以自由组合插件甚至自定义开发功能；', '允许用户根据具体业务需求扩展产品，打造最适合服务企业的解决方案。']
}, {
  label: '无界模式，跨形态打通数据',
  desc: ['支持在一个工作区内插入文档、表格、幻灯片等不同形态；', '在单元格内也可以使用有序/无序列表，在文档内也支持嵌入公式；', '把一个工作区当作一个项目来管理，在有限空间内承载更多上下文。']
}, {
  label: '协同办公，团队作战更默契',
  desc: ['支持多人同时编辑文档，可选参与者视角跟随浏览文档信息；', '插入评论定位待讨论问题，在文档内即时发起脑暴，高效沉淀关键结论。']
}]
---

<section class="features">
  <section class="preview">
    <img class="frame" src="/images/frame.png" alt="frame" draggable={false}>
    <img class="sheet" src="/images/sheets-02.png" alt="univer sheets" draggable={false} />
  </section>

  <section class="block">
    <section class="wrapper">
      <div class="list">
        <h2 class="title">
          <span>功能抢先看</span>
        </h2>

        <ul>
          {features.map(feature => (
            <li data-univer-feature>
              <a>
                <label>{feature.label}</label>
                <p>
                  {feature.desc?.map(text => (
                    <span>{text}</span>
                  ))}
                </p>
              </a>
            </li>
          ))}
        </ul>
      </div>
    </section>
  </section>
</section>

<script>
const $features = document.querySelectorAll('[data-univer-feature]')

$features.forEach(($feature, index: number) => {
  $feature.addEventListener('click', () => {
    focusFeature(index)
  })
})

function focusFeature (index: number) {
  for (let i = 0; i < $features.length; i++) {
    const $feature = $features[i]

    if (i === index) {
      $feature.classList.add('feature-active')
    } else {
      $feature.classList.remove('feature-active')
    }
  }
}

focusFeature(0)
</script>

<style lang="less" is:global>
.feature-active {
  color: #1F2327 !important;

  p {
    padding-top: 0.6rem;
    height: auto !important;
  }
}
</style>

<style lang="less" scoped>
.features {
  position: relative;
  height: 36.4rem;
  margin-bottom: 8.5rem;
  display: flex;
  align-items: center;

  .preview {
    position: absolute;
    left: 60%;
    top: 0;
    height: 38.4rem;
    width: 80.6rem;
    transform: translateX(-100%);
    z-index: 1;

    .frame {
      position: absolute;
      height: 100%;
      width: 100%;
    }

    .sheet {
      position: absolute;
      top: 3rem;
      right: 8.2rem;
      width: 63.8rem;
      height: 32.9rem;
    }
  }

  .block {
    position: relative;
    width: 100%;
    height: 28rem;
    background: rgba(228, 244, 254, 0.55);
    display: flex;
    justify-content: flex-end;

    .wrapper {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 20px;
      flex: 1;

      .list {
        position: absolute;
        left: 50%;
        height: 100%;
        padding-left: 10%;
        display: flex;
        justify-content: center;
        flex-direction: column;

        .title {
          margin: 0 0 1.4rem;

          span {
            font-size: 2.4rem;
            background-image: linear-gradient(121deg, #0048FF 18.89%, #0C81ED 39.58%, #029DCE 59.87%, #00BBB0 74.37%, #00C5A8 79.64%);
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
          }
        }

        ul {
          margin: 0;
          padding: 0;
          list-style: none;
          display: grid;
          gap: 1.2rem;

          li {
            color: #BCBCBC;
            font-size: 1rem;
            transition: color .15s;

            &:hover {
              color: #1F2327;
            }

            &::before {
              content: "";
              width: 8px;
              height: 8px;
              margin-right: 0.6rem;
              background: currentColor;
              border-radius: 50%;
              display: inline-block;
            }

            label {
              font-weight: 500;
              cursor: pointer;
            }

            p {
              margin: 0;
              height: 0;
              padding-left: 1.4rem;
              color: #999;
              font-size: 0.8rem;
              line-height: 1.8;
              overflow: hidden;
              transition: all 0.15s;

              span {
                display: block;
              }
            }
          }
        }
      }
    }
  }
}
</style>
