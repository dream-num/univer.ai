import { Callout } from 'nextra/components'

# Internationalization

Univer Pro's internationalization is built upon Univer's i18n system. You can easily use Univer Pro's i18n by simply adding the language packs provided by the Univer Pro plugin.

Some of Univer Pro's official plugins, especially those involving UI elements, include both Simplified Chinese and English language packs. You can choose the language pack that suits your needs.

<Callout type="info" emoji="ℹ️">
  If you're unfamiliar with Univer's i18n system, you can check out the [Internationalization](/guides/sheet/getting-started/i18n) section for a detailed explanation.
</Callout>

### Add Language Packs

```diff
import { LocaleType, Tools } from '@univerjs/core';
import { enUS as UniverDesignEnUS } from '@univerjs/design';
import { enUS as UniverDocsUIEnUS } from '@univerjs/docs-ui';
import { enUS as UniverSheetsEnUS } from '@univerjs/sheets';
import { enUS as UniverSheetsUIEnUS } from '@univerjs/sheets-ui';
import { enUS as UniverUIEnUS } from '@univerjs/ui';
+ import { enUS as CollaborationClientEnUS } from '@univerjs-pro/collaboration-client';
+ import { enUS as UniverSheetsDataConnectorEnUS } from '@univerjs-pro/sheets-data-connector';
+ import { enUS as SheetsPrintEnUS } from '@univerjs-pro/sheets-print';
+ import { enUS as UniverSheetsExchangeClientEnUS } from '@univerjs-pro/sheets-exchange-client';


const univer = new Univer({
  theme: defaultTheme,
  locale: LocaleType.EN_US,
  locales: {
    [LocaleType.EN_US]: Tools.deepMerge(
      UniverSheetsEnUS,
      UniverDocsUIEnUS,
      UniverSheetsUIEnUS,
      UniverUIEnUS,
      UniverDesignEnUS,
+     CollaborationClientEnUS,
+     UniverSheetsDataConnectorEnUS,
+     SheetsPrintEnUS,
+     UniverSheetsExchangeClientEnUS,
    ),
  },
});
```
