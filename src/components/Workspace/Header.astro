<header class="header">
  <nav class="nav">
    <a class="logo" href="/workspace">
      <img src="/logo.svg" draggable={false} />
    </a>

    <a class="button">
      Join Waitlist
    </a>
  </nav>
</header>

<style lang="less">
.header {
  position: sticky;
  top: 0;
  height: 68px;
  background: #FFF;
  border: 1px solid #ECEDED;
  box-shadow: 0px 4px 24px 0px rgba(31, 35, 39, 0.04);
  z-index: 1;

  .nav {
    max-width: 1200px;
    height: 100%;
    padding: 0 24px;
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;

    .logo {
      img {
        display: block;
      }
    }

    .button {
      height: 36px;
      padding: 0 24px;
      line-height: 36px;
      color: #f8f9fc;
      font-size: 14px;
      font-weight: 600;
      border-radius: 32px;
      background: linear-gradient(90deg, #5357ED 0%, #40B9FF 104.41%);
      &:hover {
        background: linear-gradient(90deg, #5357ed 0%, #5357ED 104.41%);
      }
    }
  }
}
</style>
