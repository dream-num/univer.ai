import { Tabs } from 'nextra/components'
import BadgeGroup, { UniverTypes } from '@/components/BadgeGroup'
import Sandbox from '@/components/Sandbox'

import entryFile from './filter/demo-1/index.txt'

# Filter

<BadgeGroup values={[UniverTypes.SHEET]} value={UniverTypes.SHEET} />

<Sandbox entryFile={entryFile} />

The filter plugin provides filtering functionality for cells.

## Installation

<Tabs items={['pnpm', 'npm']}>
  <Tabs.Tab label="pnpm">
    ```shell
    pnpm add @univerjs/sheets-filter @univerjs/sheets-filter-ui
    ```
  </Tabs.Tab>
  <Tabs.Tab label="npm">
    ```shell
    npm install @univerjs/sheets-filter @univerjs/sheets-filter-ui
    ```
  </Tabs.Tab>
</Tabs>

## Import

```typescript
import '@univerjs/sheets-filter-ui/lib/index.css';

import { UniverSheetsFilterPlugin } from '@univerjs/sheets-filter';
import { UniverSheetsFilterUIPlugin } from '@univerjs/sheets-filter-ui';
```

## Internationalization

```typescript
import { LocaleType, Tools } from '@univerjs/core';
import SheetsFilterUIEnUS from '@univerjs/sheets-filter-ui/locale/en-US';

const univer = new Univer({
  theme: defaultTheme,
  locale: LocaleType.EN_US,
  locales: {
    [LocaleType.EN_US]: Tools.deepMerge(
      SheetsFilterUIEnUS
    ),
  },
});
```

## Register

```typescript
univer.registerPlugin(UniverSheetsFilterPlugin);
univer.registerPlugin(UniverSheetsFilterUIPlugin);
```

## API

### Plugin API

* [@univerjs/sheets-filter](/typedoc/@univerjs/sheets-filter/README)
* [@univerjs/sheets-filter-ui](/typedoc/@univerjs/sheets-filter-ui/README)

### Facade API

* `FRange`'s properties
  * [`getFilter()`](/typedoc/@univerjs/facade/classes/FRange#getfilter)
  * [`createFilter()`](/typedoc/@univerjs/facade/classes/FRange#createfilter)
* `FWorksheet`'s properties
  * [`getFilter()`](/typedoc/@univerjs/facade/classes/FWorksheet#getfilter)
* [`FFilter`](/typedoc/@univerjs/facade/classes/FFilter)
