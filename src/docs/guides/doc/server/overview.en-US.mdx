import BadgeGroup, { UniverTypes } from '@/components/BadgeGroup'

# Overview

<BadgeGroup values={[UniverTypes.SHEET, UniverTypes.DOC]} value={UniverTypes.DOC} />

For security, performance, and other reasons, some of Univer's features are implemented in the backend service. If you use the following features, you need to refer to the subsequent documents for service deployment

- Collaborative editing
