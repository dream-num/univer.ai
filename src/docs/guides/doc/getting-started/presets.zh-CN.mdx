import { Callout } from 'nextra/components'
import VersionBadge from '@/components/VersionBadge'

# Presets  <VersionBadge version="0.5.0+" />

从 0.5.0 版本开始，我们为一些常见的场景准备了预置的插件组合以及配置，让你可以立即开始开发 Univer，不用再关心安装、引入 Facade API 和注册插件等细节。

<Callout type="info" emoji="🛸">
  如果你希望更灵活地引入和配置插件（例如懒加载部分插件，以及使用一些 Presets 不提供的配置项），可以参考 [高级引入方式](/guides/doc/getting-started/advanced-installation)。
</Callout>

<Callout type="error">
TODO: 提供基于包管理器和使用 HTML 的两个 demo
</Callout>

## 基础文档

创建一个简单的电子表格，并引用所有 Univer 提供的第一方功能（插件）。

```js
import { createUniver, defaultTheme, LocaleType } from '@univerjs/presets';
import { zhCN } from '@univerjs/presets/docs/docs-basic/zh-CN';
import { UniverDocsBasicPreset } from '@univerjs/presets/presets/docs/docs-basic/index.js';

const { univerAPI } = createUniver({
    locale: LocaleType.ZH_CN,
    theme: defaultTheme,
    presets: [
        UniverDocsBasicPreset({ locales: { zhCN } }),
    ],
});

univerAPI.createUniverDoc({});
```

在线例子

## 带协同编辑功能的文档

```js
import { createUniver, defaultTheme, LocaleType } from '@univerjs/presets';
import { zhCN as basicZhCN } from '@univerjs/presets/docs/docs-basic/zh-CN';
import { UniverDocsCollaborationPreset } from '@univerjs/presets/docs/docs-collaboration/index';
import { zhCN as collaborationZhCH } from '@univerjs/presets/docs/docs-collaboration/zh-CN';
import { UniverDocsBasicPreset } from '@univerjs/presets/presets/docs/docs-basic/index.js';

createUniver({
    locale: LocaleType.ZH_CN,
    theme: defaultTheme,
    collaboration: true,
    presets: [
        UniverDocsBasicPreset({ locales: { zhCN: basicZhCN }, collaboration: true }),
        UniverDocsCollaborationPreset({ locales: { zhCN: collaborationZhCH } }),
    ],
});
```

在线例子

## 提交建议

我们非常欢迎你试用 Presets，如果你发现 Presets 无法满足你的配置需求，可以在 [GitHub](https://github.com/dream-num/univer-presets/issues/new) 上向我们提交 Feature Request，我们会综合评估社区的需要有选择性地将更多底层的配置能力开发给 Presets。

